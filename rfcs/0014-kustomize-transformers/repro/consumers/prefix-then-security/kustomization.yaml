apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../app
components:
  # breaks: rename first → select.name: storefront matches nothing
  - ../../components/prefix
  - ../../components/security
